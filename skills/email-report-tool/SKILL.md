---
name: email-report-tool
description: Automatically formats daily Markdown logs into clean HTML emails and sends them to Gmail.
metadata:
  clawdbot:
    version: 1.0.0
---

# Email Report Tool

This tool converts the daily `memory/YYYY-MM-DD.md` log into a readable HTML format and sends it to a specified Gmail address using `gogcli`.

## Usage
`node src/index.js`
